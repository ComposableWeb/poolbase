{
  "name": "poolbase",
  "version": "0.1.0",
  "description": "Information pool for groups and companies",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy",
    "build-install-deps": "cd \"dist/app/functions\" && yarn install",
    "preserve": "npx lerna --scope poolbase-app run build && yarn build-install-deps && npx lerna --scope poolbase-landing run build",
    "serve": "cross-env NODE_ENV=production firebase serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Andreas Sahle <andreas.sahle@gmail.com> (https://pixelmord.de/)",
  "license": "MIT",
  "devDependencies": {
    "cross-env": "7.0.0",
    "firebase-tools": "^7.12.1",
    "docsify": "^4.10.2",
    "docsify-cli": "^4.4.0",
    "lerna": "^3.20.2"
  }
}
